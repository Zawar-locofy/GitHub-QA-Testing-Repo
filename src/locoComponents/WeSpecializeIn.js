import styles from "./WeSpecializeIn.module.css";

const WeSpecializeIn = () => {
  return (
    <div className={styles.weSpecializeIn}>
      We specialize in developing custom applications that streamline processes
      and improve efficiency for businesses of all sizes.
    </div>
  );
};

export default WeSpecializeIn;
