import { FunctionComponent } from "react";

const WeSpecializeIn: FunctionComponent = () => {
  return (
    <div className="absolute top-[183.87px] left-[134.95px] text-xs leading-[163%] font-paragraph inline-block w-[251.05px]">
      We specialize in developing custom applications that streamline processes
      and improve efficiency for businesses of all sizes.
    </div>
  );
};

export default WeSpecializeIn;
